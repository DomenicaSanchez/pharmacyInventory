---
import React from "react";
import ListProduct from "./ListProduct.jsx";

const { products, onEdit, onView } = Astro.props;
---

<ListProduct products={products} onEdit={onEdit} onView={onView} client:load />
